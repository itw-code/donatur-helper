# Keep bulk actions secondary and confirmed

**Status:** accepted

Existing bulk operations remain available as secondary controls. They must expose the affected count, require explicit confirmation, and be hidden or disabled when the operation is unsafe, such as when a proof selection is incomplete.
