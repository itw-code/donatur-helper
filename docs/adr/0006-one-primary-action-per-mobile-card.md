# Use one primary action per mobile card

Each mobile card will expose one visually dominant action that advances the user’s current task; secondary actions remain available with lower emphasis, while destructive actions are separated into a danger area and confirmed before execution. This reduces cramped action rows and accidental destructive taps across Donor, PIC, Admin, and SuperAdmin workflows.
