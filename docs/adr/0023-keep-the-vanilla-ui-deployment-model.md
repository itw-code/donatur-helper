# Keep the vanilla UI deployment model

The redesign will continue using the existing HTML, CSS, and JavaScript structure, with reusable CSS classes, small UI helpers, and inline SVG icons rather than introducing React, Tailwind, or a component framework. `Code.js` and the deployment model remain unchanged so the work improves the interface without creating a technology migration.
