# Use consistent accessible mobile form validation

All Donor, PIC, Admin, and SuperAdmin forms will use visible labels, semantic input types, validation on blur or submit rather than every keystroke, field-level error messages, focus on the first invalid field, and preservation of entered values after a failed request. This makes form recovery predictable on small screens and with mobile keyboards.
