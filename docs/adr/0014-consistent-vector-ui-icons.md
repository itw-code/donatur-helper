# Use consistent vector icons for structural UI

Structural interface icons will use a consistent inline SVG icon language instead of emoji characters. This applies to navigation, search, loading, alerts, copy actions, status indicators, and controls; meaningful labels remain visible where an icon alone would be ambiguous. This avoids platform-dependent rendering and improves alignment, contrast, and accessibility.
