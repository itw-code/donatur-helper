# Prefer native date inputs on mobile

Date fields will use the browser’s native mobile date control for touch and accessibility, while enhanced calendar behavior may remain for larger screens. Both paths must preserve the same value format and validation rules. This avoids forcing a desktop-style calendar interaction onto phone users.
