# Preserve shared Campaign context through authentication

When a Donor opens a shared Campaign link, the application will preserve that Campaign context through login or registration and return the Donor directly to the Campaign with `Gabung Donasi` as the primary action. This supports the invitation-driven mobile entry path and avoids forcing users to search for the Campaign again after authentication.
