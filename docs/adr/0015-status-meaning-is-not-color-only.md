# Communicate status with labels and non-color cues

Campaign, payment, approval, and deadline states will use semantic color as a supporting cue, never as the only meaning. Each state must also expose a readable label and, where useful, an icon or shape so the status remains understandable across themes, contrast settings, and color-vision differences.
