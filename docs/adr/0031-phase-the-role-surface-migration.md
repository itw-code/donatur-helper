# Phase the role-surface migration

**Status:** accepted

The UI migration will ship in phases rather than one coordinated cutover across every role. Each phase must be independently usable and verified before the next role surface is migrated, reducing regression risk while the existing monolithic frontend is reshaped.
