# Cover all Campaign lifecycle states in Phase 1

**Status:** accepted

Phase 1 will migrate authenticated Donor and PIC workflows for Open, Finalized, and Closed Campaigns. Each state keeps its existing capabilities but receives state-appropriate action priority; the migration will not leave users on a legacy visual path merely because a Campaign has moved to a different lifecycle state.
