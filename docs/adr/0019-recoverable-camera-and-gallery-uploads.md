# Support recoverable camera and gallery uploads on mobile

Mobile proof and gift-documentation uploads will support both camera capture and gallery or file selection, without forcing either path. The UI will provide a preview, upload progress, retry feedback, and preservation of the surrounding form state; safe client-side image resizing or compression may be used to reduce mobile upload cost.
