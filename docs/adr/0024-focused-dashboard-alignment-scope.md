# Keep dashboard alignment focused on current role surfaces

The current dashboard-alignment work will fix concrete Donor, PIC, and Admin issues without expanding into a navigation redesign or a SuperAdmin action-priority pass. Contextual mobile navigation and SuperAdmin prioritization are intentionally deferred to a separate design and implementation decision so this change remains focused and reviewable. The personalized Admin greeting is removed as unrelated scope; the Admin action queue remains the focused Admin change.
