# Use contextual bottom sheets on mobile

Short confirmations, contextual details, and compact forms will use accessible bottom sheets on mobile; large or multi-step workflows will use full-screen views. Every sheet keeps visible close or cancel controls and proper focus management, and no critical action depends on swipe-to-dismiss behavior. This preserves context without turning primary navigation into a modal flow.
