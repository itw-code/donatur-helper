# Require a visible reason for rejected proof

**Status:** accepted

A PIC must provide a short reason when rejecting a Bukti Transfer, and the Donor must see that reason before submitting a replacement. The reason is part of the re-upload workflow rather than an internal-only audit note; if the current data contract cannot store it, that gap must be specified separately.
