# Scope the new design foundation by role during migration

**Status:** accepted

During Phase 1, new visual tokens and components should be scoped to Donor/PIC surfaces so the existing Admin/SuperAdmin experience remains stable. Shared foundations may be extracted only when they are demonstrably neutral to legacy surfaces; global replacement waits until the later role migration.
