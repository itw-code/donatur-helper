# Use native sharing with copy fallback

**Status:** accepted

The PIC share affordance will use the native device share sheet when available and fall back to the existing copy-invitation capability when it is not. It must not introduce a separate messaging workflow solely to match the prototype.
