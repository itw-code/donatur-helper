# Use contextual navigation instead of persistent bottom navigation

Mobile navigation will use a contextual top bar with the current view, predictable back behavior, and relevant account or action controls; the app will not add a persistent bottom navigation bar. The product is organized around role-specific workflows rather than a stable set of shared top-level destinations, so a bottom bar would add clutter and falsely imply the same navigation model for every role.
