# Netlify configuration to permanently redirect all traffic to Cloudflare Pages.
# This file only exists/runs on Netlify — Cloudflare Pages never reads it,
# so there is no redirect loop risk.

[[redirects]]
  from = "/*"
  to = "https://don4tpro.pages.dev/:splat"
  status = 301
  force = true
